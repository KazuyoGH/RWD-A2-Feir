1. Install Dependencies
Open your terminal in the project root folder and run:
`npm install`

2. Database Setup
Before starting the server, you need to populate your local database with test data:
`node seed.js`

3. Start the Server
Once the data is seeded, launch the application:
`node server.js`

4. Important Credentials
To access the dashboard via the login page, use the following test credentials:
Username: maryd
Password: 123password

--or--

Username: adamp
Password: password123

**The portal will now be live at http://localhost:3000.**